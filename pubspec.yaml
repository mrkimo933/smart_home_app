name: smart_home_app
description: Smart Home Energy Monitor
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.9
  mqtt_client: ^10.0.0
  sqflite: ^2.3.0
  path_provider: ^2.1.2
  fl_chart: ^0.65.0
  flutter_local_notifications: ^17.0.0
  shared_preferences: ^2.2.2
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
